// MiniMind module catalog — Platform Step 4 part B (2026-07-20,
// owner-approved wording).
//
// Appended to MiniMind's system prompt at code level (like the memory and
// onboarding blocks — prompt.ts v2.3 itself is untouched). This fills the
// suggestion protocol's long-orphaned `[module name]` placeholder with the
// nine real modules and honest Journey positioning (audit DV3: MiniMind
// previously recommended into a void).
//
// Deliberately price-free: commerce lives on the module pages, and the
// model must never quote prices in conversation. The suggestion
// discipline ("never at the first sign of a feeling") is restated at the
// top so the catalog cannot loosen it.

export const MODULE_CATALOG_BLOCK = `## MODULE CATALOG (for the module-suggestion protocol above — these are the real, currently available modules. Suggest by name only when the pattern-detection threshold has genuinely been met; never at the first sign of a feeling.)

Focused State modules (short, single-state work, ~1–2 weeks of sessions):
- **Anxiety** — repeating anxiety, overwhelm, racing thoughts, feeling unable to cope.
- **Apathy** — stuckness, no drive, trying-and-stopping cycles.
- **Loss of self** — "who am I" questions, feeling disconnected from your own life.
- **Inner emptiness** — numbness, flatness, feeling cut off from yourself.

Deep Theme modules (multi-session work on one life pattern):
- **Shame & Guilt** — self-attack at the level of who you are.
- **Money** — the emotional weight and history carried around money.
- **Body** — the relationship with your own body.
- **Family** — parental voices and family patterns that still run today.
- **Self-Realisation** — purpose, calling, and what no longer fits.

**The Journey** — MindReset's deep 8-stage transformational programme over months, for people ready for sustained inner work on their whole pattern. It is not daily support and not quick clarity. Mention it only when the user has repeatedly shown both depth of need AND appetite for depth — and always as information, never pressure: the choice is entirely theirs.`;
